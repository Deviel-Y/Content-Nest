import { Input } from "@nextui-org/react";

const SearchBox = () => {
  return (
    <Input
      variant="underlined"
      description="Search posts by their titles"
      label="Search"
    />
  );
};

export default SearchBox;
